{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/icecolor78-dev/ai-repository-audit/schemas/rem-v1.1.schema.json",
  "title": "Repository Evidence Mapper v1.1 Portrait",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "subject", "inventory", "ci", "tests", "security", "release", "supply_chain", "claims", "provenance", "coverage", "sources"],
  "properties": {
    "schema_version": {"const": "rem/v1.1"},
    "subject": {"$ref": "#/$defs/subject"},
    "inventory": {"$ref": "#/$defs/inventory"},
    "ci": {"$ref": "#/$defs/ci"},
    "tests": {"$ref": "#/$defs/tests"},
    "security": {"$ref": "#/$defs/security"},
    "release": {"$ref": "#/$defs/release"},
    "supply_chain": {"$ref": "#/$defs/supplyChain"},
    "claims": {"$ref": "#/$defs/claims"},
    "provenance": {"$ref": "#/$defs/provenance"},
    "coverage": {"$ref": "#/$defs/coverage"},
    "sources": {"type": "array", "minItems": 1, "items": {"$ref": "#/$defs/source"}}
  },
  "$defs": {
    "state": {"enum": ["VERIFIED", "PARTIAL", "UNVERIFIED", "CONTRADICTED", "NOT_APPLICABLE"]},
    "strings": {"type": "array", "items": {"type": "string"}},
    "signal": {
      "type": "object", "additionalProperties": false,
      "required": ["kind", "locator", "detail", "confidence", "severity"],
      "properties": {
        "kind": {"type": "string", "minLength": 1}, "locator": {"type": "string", "minLength": 1},
        "detail": {"type": "string"}, "confidence": {"type": "string"}, "severity": {"type": "string"}
      }
    },
    "signals": {"type": "array", "items": {"$ref": "#/$defs/signal"}},
    "subject": {
      "type": "object", "additionalProperties": false,
      "required": ["provider", "repository", "revision", "default_branch", "observed_at", "visibility"],
      "properties": {
        "provider": {"const": "github"}, "repository": {"type": "string", "pattern": "^[^/\\s]+/[^/\\s]+$"},
        "revision": {"type": "string", "pattern": "^[0-9a-f]{40}$"}, "default_branch": {"type": "string", "minLength": 1},
        "observed_at": {"type": "string", "format": "date-time"}, "visibility": {"enum": ["public", "private"]}
      }
    },
    "inventory": {
      "type": "object", "additionalProperties": false,
      "required": ["languages", "manifests", "lockfiles", "modules", "generated_or_vendor_boundaries"],
      "properties": {
        "languages": {"$ref": "#/$defs/strings"}, "manifests": {"$ref": "#/$defs/strings"}, "lockfiles": {"$ref": "#/$defs/strings"},
        "modules": {"$ref": "#/$defs/strings"}, "generated_or_vendor_boundaries": {"$ref": "#/$defs/strings"}
      }
    },
    "ci": {
      "type": "object", "additionalProperties": false,
      "required": ["workflows", "required_checks", "trigger_model", "path_filters", "runtime_matrix", "os_arch_matrix", "conditional_jobs", "aggregate_checks", "exact_subject_runs", "confidence", "unknowns", "pr_confidence", "main_confidence", "release_confidence", "security_evidence_freshness"],
      "properties": {
        "workflows": {"$ref": "#/$defs/strings"}, "required_checks": {"$ref": "#/$defs/strings"}, "trigger_model": {"$ref": "#/$defs/strings"},
        "path_filters": {"$ref": "#/$defs/strings"}, "runtime_matrix": {"$ref": "#/$defs/strings"}, "os_arch_matrix": {"$ref": "#/$defs/strings"},
        "conditional_jobs": {"$ref": "#/$defs/strings"}, "aggregate_checks": {"$ref": "#/$defs/strings"}, "exact_subject_runs": {"$ref": "#/$defs/strings"},
        "confidence": {"$ref": "#/$defs/state"}, "unknowns": {"$ref": "#/$defs/strings"}, "pr_confidence": {"$ref": "#/$defs/state"},
        "main_confidence": {"$ref": "#/$defs/state"}, "release_confidence": {"$ref": "#/$defs/state"}, "security_evidence_freshness": {"$ref": "#/$defs/state"}
      }
    },
    "tests": {
      "type": "object", "additionalProperties": false,
      "required": ["suites", "types", "subsystem_map", "exact_subject_execution", "confidence", "unknowns"],
      "properties": {"suites": {"$ref": "#/$defs/strings"}, "types": {"$ref": "#/$defs/strings"}, "subsystem_map": {"$ref": "#/$defs/strings"}, "exact_subject_execution": {"$ref": "#/$defs/strings"}, "confidence": {"$ref": "#/$defs/state"}, "unknowns": {"$ref": "#/$defs/strings"}}
    },
    "security": {
      "type": "object", "additionalProperties": false,
      "required": ["workflow_permissions", "credential_persistence", "action_pinning", "static_analysis", "dependency_security", "secret_scanning_evidence", "freshness", "signals", "confidence", "unknowns"],
      "properties": {"workflow_permissions": {"$ref": "#/$defs/strings"}, "credential_persistence": {"$ref": "#/$defs/signals"}, "action_pinning": {"$ref": "#/$defs/strings"}, "static_analysis": {"$ref": "#/$defs/strings"}, "dependency_security": {"$ref": "#/$defs/strings"}, "secret_scanning_evidence": {"$ref": "#/$defs/strings"}, "freshness": {"$ref": "#/$defs/strings"}, "signals": {"$ref": "#/$defs/signals"}, "confidence": {"$ref": "#/$defs/state"}, "unknowns": {"$ref": "#/$defs/strings"}}
    },
    "release": {
      "type": "object", "additionalProperties": false,
      "required": ["workflows", "artifact_handoffs", "source_artifact_binding", "trusted_publishing", "signing_or_attestation", "smoke_or_release_tests", "rollback_or_recovery", "signals", "confidence", "unknowns"],
      "properties": {"workflows": {"$ref": "#/$defs/strings"}, "artifact_handoffs": {"$ref": "#/$defs/signals"}, "source_artifact_binding": {"$ref": "#/$defs/strings"}, "trusted_publishing": {"$ref": "#/$defs/signals"}, "signing_or_attestation": {"$ref": "#/$defs/signals"}, "smoke_or_release_tests": {"$ref": "#/$defs/signals"}, "rollback_or_recovery": {"$ref": "#/$defs/signals"}, "signals": {"$ref": "#/$defs/signals"}, "confidence": {"$ref": "#/$defs/state"}, "unknowns": {"$ref": "#/$defs/strings"}}
    },
    "supplyChain": {
      "type": "object", "additionalProperties": false,
      "required": ["dependencies", "update_automation", "sbom", "license_evidence", "provenance", "confidence", "unknowns"],
      "properties": {"dependencies": {"$ref": "#/$defs/strings"}, "update_automation": {"$ref": "#/$defs/strings"}, "sbom": {"$ref": "#/$defs/strings"}, "license_evidence": {"$ref": "#/$defs/strings"}, "provenance": {"$ref": "#/$defs/strings"}, "confidence": {"$ref": "#/$defs/state"}, "unknowns": {"$ref": "#/$defs/strings"}}
    },
    "claim": {
      "type": "object", "additionalProperties": false,
      "required": ["claim", "source_ref", "state", "supporting_refs", "contradicting_refs", "notes"],
      "properties": {"claim": {"type": "string"}, "source_ref": {"type": "string"}, "state": {"$ref": "#/$defs/state"}, "supporting_refs": {"$ref": "#/$defs/strings"}, "contradicting_refs": {"$ref": "#/$defs/strings"}, "notes": {"type": "string"}}
    },
    "claims": {"type": "object", "additionalProperties": false, "required": ["items"], "properties": {"items": {"type": "array", "items": {"$ref": "#/$defs/claim"}}}},
    "provenance": {
      "type": "object", "additionalProperties": false,
      "required": ["ai_assisted_changes", "generated_code", "external_build_or_test_services", "confidence", "unknowns"],
      "properties": {"ai_assisted_changes": {"$ref": "#/$defs/strings"}, "generated_code": {"$ref": "#/$defs/strings"}, "external_build_or_test_services": {"$ref": "#/$defs/strings"}, "confidence": {"$ref": "#/$defs/state"}, "unknowns": {"$ref": "#/$defs/strings"}}
    },
    "coverage": {
      "type": "object", "additionalProperties": false,
      "required": ["dimensions", "explicit_unknowns", "excluded_scope"],
      "properties": {"dimensions": {"$ref": "#/$defs/strings"}, "explicit_unknowns": {"$ref": "#/$defs/strings"}, "excluded_scope": {"$ref": "#/$defs/strings"}}
    },
    "source": {
      "type": "object", "additionalProperties": false,
      "required": ["id", "kind", "subject_revision", "locator", "observed_at", "freshness", "access", "supports", "limits"],
      "properties": {
        "id": {"type": "string", "minLength": 1}, "kind": {"enum": ["repository_file", "workflow_definition", "workflow_run", "workflow_job", "commit", "status", "release", "issue", "pull_request", "external"]},
        "subject_revision": {"type": ["string", "null"], "pattern": "^[0-9a-f]{40}$"}, "locator": {"type": "string", "minLength": 1}, "observed_at": {"type": "string", "format": "date-time"},
        "freshness": {"type": "object", "additionalProperties": false, "required": ["state", "reason"], "properties": {"state": {"enum": ["exact", "current-but-not-exact", "historical", "unknown"]}, "reason": {"type": "string"}}},
        "access": {"type": "object", "additionalProperties": false, "required": ["state"], "properties": {"state": {"enum": ["accessible", "partial", "inaccessible"]}}},
        "content_digest": {"type": "string"}, "supports": {"$ref": "#/$defs/strings"}, "limits": {"$ref": "#/$defs/strings"}
      }
    }
  }
}
